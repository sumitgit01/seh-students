apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "manifestbuild.fullname" . }}-test-connection"
  labels:
    {{- include "manifestbuild.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "manifestbuild.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
